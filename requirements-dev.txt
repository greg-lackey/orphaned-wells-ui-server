black==22.3.0
pytest==8.4.2
pytest-mock>=3.12.0
setuptools==69.0.3

# ETL / sql_sync dependencies
-r sql_sync/requirements.txt

# Full server stack (requires grpcio — needs Python <=3.13 or a pre-built wheel)
# Uncomment to install for server development:
# -e .[dev]
